images:
  image_name: containerregistry2i2cutoronto.azurecr.io/ut-user-image
  registry:
    provider: dockerconfig
    dockerconfig:
      filename: dockerconfig.json

cluster:
  provider: kubeconfig
  kubeconfig:
    filename: kubeconfig.yaml